# Phase 0 — Tasks: Foundation & Infrastructure

> **Status: COMPLETE** ✅ — All tasks finished 2026-04-04

## Task Checklist

### 1. Project Skeleton
- [x] Initialise Python package with `pyproject.toml` using UV
- [x] Configure `[project.scripts]` entry point: `job-hunter = "job_hunter.cli:main"`
- [x] Set up `src/` layout with `job_hunter` package
- [x] Create `.env.example` with required variable names
- [x] Add `.gitignore` (Python, .env, data, output, .venv)

### 2. Configuration Layer
- [x] Define `AppConfig` Pydantic model with nested `Profile`, `SearchConfig`, `ScoringConfig`, `ScreeningAnswers`
- [x] Implement `load_config(path)` function with YAML parsing
- [x] Create `config/user.yaml` template (generated by `init` command)

### 3. CLI Entry Point
- [x] Implement `job-hunter init` — creates directories and config template
- [x] Implement `job-hunter run` — accepts `--resume`, `--config`, `--headless` flags
- [x] Implement `job-hunter status` — shows cached profile and CSV export history
- [x] Verify `--help` output for all commands

### 4. LLM Provider
- [x] Create `LLMProvider` singleton class
- [x] Wire Groq `llama-3.3-70b-versatile` as primary LLM
- [x] Wire OpenAI `gpt-4o` as fallback LLM
- [x] Implement auto-switch logic on `rate_limit` / `429` / `quota` / `overloaded` errors
- [x] Load credentials from `.env` via `python-dotenv`

### 5. Browser Manager
- [x] Create `BrowserManager` class with `start()` and `close()` lifecycle methods
- [x] Configure anti-bot evasion: disable `webdriver`, spoof plugins, set locale/timezone
- [x] Implement `login_naukri()` using credentials from `.env`
- [x] Return `Page` object for injection into LangGraph state

### 6. LangGraph Foundation
- [x] Define `JobHunterState` TypedDict in `graph/state.py`
- [x] Create `workflow.py` StateGraph shell with node stubs
- [x] Confirm `build_workflow()` compiles without errors

### 7. SDD Framework
- [x] Create full `specs/` directory tree (backlog, decisions, phases, roadmap, vision, architecture)
- [x] Write vision docs: `project-charter.md`, `principles.md`, `success-criteria.md`
- [x] Write architecture overview: `specs/architecture/overview.md`
- [x] Configure OpenCode: `.opencode/` commands, plugins, `opencode.json`
- [x] Write `CLAUDE.md` with always-on agent rules
- [x] Implement SDD compliance scripts: `scripts/check-sdd-compliance.sh`, `scripts/pre-commit`
- [x] Install git hooks path: `git config core.hooksPath scripts/`

## Verification Steps

- [x] Run `uv run job-hunter init` — no errors, config created
- [x] Run `uv run job-hunter status` — no errors
- [x] Run `uv run job-hunter run --help` — all flags shown
- [x] `from job_hunter.config import AppConfig` — importable
- [x] `from job_hunter.llm.provider import LLMProvider` — importable
- [x] `from job_hunter.browser import BrowserManager` — importable
- [x] `from job_hunter.graph.workflow import build_workflow` — importable, compiles
