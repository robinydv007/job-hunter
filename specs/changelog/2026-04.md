# Changelog — 2026-04

## 2026-04-09

### Doc Sync - Phase 2b Replanning
- **Phase 2b status updated to NOT STARTED** — Implementation reverted for replanning
- **Tasks.md prerequisites checked** — Phase 2a items now marked complete
- **Phase index.json updated** — Phase 2b status changed to "not_started", added new keywords (state-machine, contenteditable, sequential-forms)
- **Overview.md updated** — Added "Challenges Faced" section documenting implementation learnings
- **Plan.md rewritten** — Complete rewrite with state machine approach (read-analyze-act loop)

## 2026-04-08

### Phase 3.0 — Async Architecture Foundation (Spec Created)
- **Phase 3.0 specification created** — Complete async architecture foundation spec:
  - Created `specs/phases/phase-3.0-async-foundation/overview.md` — Goal, scope, architecture overview
  - Created `specs/phases/phase-3.0-async-foundation/plan.md` — Step-by-step implementation plan
  - Created `specs/phases/phase-3.0-async-foundation/tasks.md` — Detailed task checklist
  - Created `specs/phases/phase-3.0-async-foundation/history.md` — Phase history tracking
- **Goal**: Resolve ENH-017 (reorder pipeline: parse resume before login) by converting entire pipeline from sync+nest_asyncio to pure async
- **Scope**: Async conversion of all nodes, functions, and search operations; add login node; reorder pipeline; remove nest-asyncio dependency

### Phase 2b — Auto-Apply (Replanning)
- **Phase 2b implementation reverted** — Removed implementation attempt to replan
- **Challenges documented** in overview.md:
  - Contenteditable divs instead of input elements
  - Sequential question flow (questions shown one at a time)
  - Clickable option divs (not standard selects)
  - Submit button blocking by chatbot overlay
  - Resume upload failures (need skip fallback)
- **Plan updated** — Changed from batch approach to state machine (read-analyze-act loop)
- **Tasks.md marked as NOT STARTED**
- **Note**: Naukri search works normally in non-headless browser mode

### Documentation Updates
- Updated `specs/phases/phase-2b-auto-apply/tasks.md` - status to NOT STARTED
- Updated `specs/phases/phase-2b-auto-apply/history.md` - added implementation attempt and challenges
- Updated `specs/phases/phase-2b-auto-apply/overview.md` - added challenges section and revised approach
- Updated `specs/phases/phase-2b-auto-apply/plan.md` - complete rewrite with state machine approach
- Updated `specs/roadmap/roadmap.md` - added Phase 3.0 and 3.1 entries and dependencies
- Updated `specs/backlog/details/ENH-017.md` - updated reference to Phase 3.0 implementation
- Updated `specs/phases/index.json` - added Phase 3.0 entry with keywords
- Updated `specs/status.md` - added reference to Phase 3.0

## 2026-04-07

### Phase 2a — Detailed Profile & Config Restructure (Complete)
- **Phase 2a complete** — Detailed profile and config restructure delivered:
  - Create `config/screening.yaml` with screening_answers moved from user.yaml
  - Add `resume_path` to Profile model with default `resume.pdf`
  - Implement `parse_resume_full()` for single LLM call extracting both basic + detailed profiles
  - Add `save_detailed_profile()` and `load_detailed_profile()` functions
  - Implement SHA256-based resume change detection in `data/resume_hash.txt`
  - Add `--force-parse` flag to force re-parse of resume
  - Add `job-hunter clean` command to clear cached files
  - Fix: --force-parse properly forces re-parse by passing force flag through the pipeline

### Phase 2b — Auto-Apply & Batch Screening (In Progress)
- **Phase 2b started** — Exploration completed:
  - Created Playwright exploration script to understand Naukri apply flow
  - Discovered: Apply button selector `.apply-button`, sidebar-based form (not modal/page)
  - Logged-in users see pre-filled fields: resume upload, experience dropdown, location
  - Updated overview.md with actual selectors and implementation notes
  - Tasks.md marked as in-progress, prerequisites section updated

### Enhancements
- **ENH-015: add LLM-based job scoring** — new optional scoring method enabled via `scoring.llm_scoring.enabled: true` in user.yaml; scores jobs in batches using LLM with structured JSON output (score + reasoning); configurable batch_size, shortlist_threshold, consider_location, consider_work_mode flags
- **enh(config): use preferred_roles from user.yaml everywhere** — remove dependency on profile.json target_roles; scoring engine, LLM scorer, and search now use config.profile.preferred_roles exclusively
- **ENH-016: Fix Naukri pagination using UI navigation** — replaced direct URL pagination with clicking "Next" button in pagination UI, added fallback to direct URL navigation, implemented duplicate detection to prevent infinite loops, fixed issue where pages 2+ showed duplicate results from page 1

## 2026-04-06

### Enhancements
- enh(scoring): revamp scoring engine v2 — 6 dimensions (skills 35%, role 20%, experience 20%, company 10%, location 8%, work_mode 7%); add skill aliases, word-boundary matching, concatenated skill splitting, company rating/review scoring, preferred location scoring with metro city matching, work mode preference matching; replace freshness with preferred location; inline title exclude keyword check for zero-score jobs
- enh(search): implement pagination for Naukri — loop through max_pages, construct page URLs like `/technical-lead-jobs-2-in-bangalore`, add no-results detection, configurable via `naukri.max_pages` config
- enh(config): add `max_jobs_per_query` to SearchConfig — replaces hardcoded 100 in search; default 50, fetched from `search.max_jobs_per_query` in user.yaml; final shortlist capped by `search.max_jobs` (0 = no limit)
- **enh(config): extract static data to `config/constants.yaml`** — move all domain knowledge (skill aliases, metro cities, rating bands, thresholds) to separate config file; create `Constants` Pydantic model with `load_constants()` cached loader; strip all static data from `scoring/engine.py` (~270 lines removed)
- **enh(config): add scoring weights to user.yaml** — `scoring.skill_weight`, `role_weight`, `experience_weight`, `company_weight`, `location_weight`, `work_mode_weight` now user-editable
- **enh(export): refactor CSV export with single source of truth** — replace dual definition (MVP_COLUMNS list + row dict) with `ROW_MAPPING` dict mapping column → lambda extractor; add column in one place only
- **enh(search): fix Naukri base_url bug** — `NAUKRI.base_url` placeholder string replaced with actual URL from `config/job_boards/naukri.py`

### Bug Fixes
- **fix(scoring): experience penalty key type mismatch** — YAML loads keys as integers but code was converting to string; fixed by using int keys directly instead of str()
- **fix(config): path resolution for config/__init__.py** — changed `parents[2]` to `parents[3]` to correctly locate `config/user.yaml` from `src/job_hunter/config/` directory
- **fix(console): unicode encode error on Windows** — replaced `→` arrow with `->` in Rich console output to avoid cp1252 codec error
- fix(search): add missing imports (Path, json, datetime) to naukri.py that were removed in earlier refactor

### Tech Debt
- refactor(scoring): remove dead code — `_TITLE_STOP_WORDS` (unused), `NON_TECHNICAL_SKILLS` filter (hurts accuracy), `_split_concatenated_skills` + `preserved` + `known_skill_starts` (hack for bad listing data)
- refactor(config): create `src/job_hunter/config/constants.py` — move `Constants` class and `load_constants()` from inline to separate module; add `src/job_hunter/config/job_boards/` package for platform constants
- refactor(config): create `config/constants.yaml` — domain knowledge that doesn't belong in user.yaml (not user-editable, project-wide)
- **refactor(config): move config.py to config/__init__.py** — convert single file to package; add missing imports (Path, json, datetime) to naukri.py

## 2026-04-05

### Enhancements
- enh(search): add pagination support — navigate through Naukri search pages until max_jobs limit reached or no more results
- enh(cli): add `--force-parse` flag to override resume cache and force re-parsing
- enh(cli): implement resume caching — skips LLM parse if `data/profile.json` exists and `--resume` not explicitly passed
- enh(search): priority resolution for search roles — `user.yaml` `preferred_roles` overrides parsed `target_roles`
- enh(scoring): add title tech penalty — halve score if job title names a primary tech the user lacks (e.g., "Java Technical Lead" for non-Java devs)
- enh(config): ENH-003 — expand SearchConfig with work_mode_filter, job_types, excluded_companies, excluded_keywords; add AutoApplyConfig for Phase 2; add 10 ScreeningAnswers fields
- enh(search): ENH-004 — smart freshness filter with auto-calculation from run history; configurable dd values; first run defaults to 7 days
- enh(search): ENH-005 — role-only queries (no skills), remove 10-query cap, 100 raw/query soft limit, remove 150 total cap, configurable max_roles/max_locations
- enh(anti-blocking): ENH-006 — randomize all delays, jitter sleeps, randomize scroll count/distance, configurable query delay bounds

### Tech Debt
- refactor(state): TD-004 — prune unused fields from JobHunterState; remove profile_validated, messages, errors; replace error returns with RuntimeError

### Bug Fixes
- fix(search): regression fix — use jobAge=N param for actual freshness filtering (was dd which only sorts)
- fix(search): extract skills from div.row5 on Naukri listing page; fallback to user-skill word-boundary scan in description
- fix(config): restore shortlist_threshold to 60, apply_threshold to 75, max_jobs to 10

### Bug Fixes
- fix(cli): move interactive config prompts before pipeline start — validates and prompts for missing fields before browser launch (BUG-001)

### Phase 1 — MVP Core Pipeline (v0.1.0)
- feat: implement `resume/parser.py` — LLM-based resume extraction with JSON caching (`data/profile.json`)
- feat: implement `resume/schema.py` — `ResumeProfile` Pydantic model (13 structured fields)
- feat: implement `search/naukri.py` — Playwright scraper with 7 CSS selector strategies, lazy-load scroll, within-run + cross-run deduplication
- feat: implement `scoring/engine.py` — 6-factor weighted rubric (0–100 composite), `matched_skills`, `why_selected`, `apply_status`
- feat: implement `export/csv_export.py` — timestamped CSV writer with 15-column MVP schema
- feat: implement `graph/nodes.py` — all 6 LangGraph pipeline node functions
- feat: implement `graph/workflow.py` — StateGraph compiled and wired end-to-end
- docs: write `specs/architecture/overview.md` — complete system architecture documentation
- docs: update `specs/vision/success-criteria.md` — mark Phase 1 criteria complete
- docs: add `specs/backlog/details/BUG-001.md` — interactive config prompts missing on `run`

## 2026-04-04

### Phase 0 — Foundation & Infrastructure (v0.0.1)
- feat: initialise Python package with `pyproject.toml` (UV, Pydantic v2, LangGraph, Playwright, Click, Rich, Groq, OpenAI)
- feat: implement `cli.py` — `init`, `run`, `status` commands with Click + Rich
- feat: implement `config.py` — `AppConfig` Pydantic model with YAML loading
- feat: implement `browser.py` — `BrowserManager` with anti-bot evasion and Naukri login
- feat: implement `llm/provider.py` — `LLMProvider` singleton (Groq primary, OpenAI fallback)
- feat: implement `graph/state.py` — `JobHunterState` TypedDict definition
- feat: implement `graph/workflow.py` — `StateGraph` shell for LangGraph
- infra: set up SDD spec directory tree (`specs/backlog`, `decisions`, `phases`, `roadmap`, `vision`, `architecture`)
- infra: write vision docs (`project-charter.md`, `principles.md`, `success-criteria.md`)
- infra: implement SDD enforcement scripts (`scripts/check-sdd-compliance.sh`, `scripts/pre-commit`)
- infra: configure OpenCode (`.opencode/` commands, plugins, `opencode.json`)
- docs: write `CLAUDE.md` — always-on agent rules (10 autonomous behaviors)
