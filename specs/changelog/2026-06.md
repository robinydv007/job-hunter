# Changelog — 2026-06

## 2026-06-03

### fix(search): scope pagination Next button selector to avoid matching job titles
### fix(search): freshness resolve now uses min() so same-day re-run returns 1 instead of config value
